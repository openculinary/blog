spec:
  template:
    spec:
      containers:
      - name: blog
        volumeMounts:
          - name: public
            readOnly: true
            mountPath: /usr/share/nginx/html
      volumes:
        - name: public
          persistentVolumeClaim:
            claimName: blog-pvc
