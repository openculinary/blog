spec:
  template:
    spec:
      containers:
      - name: blog
        volumeMounts:
          - name: content
            readOnly: true
            mountPath: /usr/share/nginx/html
      volumes:
        - name: content
          persistentVolumeClaim:
            claimName: blog-pvc
